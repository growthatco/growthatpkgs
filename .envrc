eval "$(lorri direnv)"

# The python version will default to the version
# defined in the root level `shell.nix` file.
layout python

# Install environment requirements
pip install -r requirements.txt